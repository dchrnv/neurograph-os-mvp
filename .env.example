# NeuroGraph OS - Environment Configuration
# Copy this file to .env and adjust values as needed

# =============================================================================
# API Server Configuration
# =============================================================================

# Logging level (trace, debug, info, warn, error)
RUST_LOG=info

# API server port (exposed to host)
NEUROGRAPH_PORT=8080

# Resource limits
NEUROGRAPH_MAX_TOKENS=10000000
NEUROGRAPH_MAX_MEMORY_BYTES=1073741824

# =============================================================================
# Monitoring (Optional)
# =============================================================================

# Grafana credentials (only used with --profile monitoring)
GRAFANA_USER=admin
GRAFANA_PASSWORD=changeme

# =============================================================================
# Distributed Tracing (Optional - v0.44.0)
# =============================================================================

# Enable distributed tracing with Jaeger
ENABLE_TRACING=false

# Jaeger endpoint (only used when ENABLE_TRACING=true with --profile tracing)
JAEGER_ENDPOINT=http://jaeger:14268/api/traces

# =============================================================================
# Notes
# =============================================================================

# To start only the API server:
#   docker-compose up -d

# To start with monitoring stack:
#   docker-compose --profile monitoring up -d

# To view logs:
#   docker-compose logs -f neurograph-api

# To stop all services:
#   docker-compose down
