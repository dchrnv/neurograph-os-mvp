# NeuroGraph Web Dashboard — Спецификация v0.62.0

**Версия:** 0.62.0  
**Дата:** 2024-12-30  
**Статус:** Спецификация для реализации  
**Исполнитель:** Claude Sonnet  

---

## 1. Обзор проекта

### 1.1 Что это

Web Dashboard для системы NeuroGraph — полноценная панель управления и мониторинга когнитивной архитектуры. Не визуализатор графов, а административный интерфейс для:

- Мониторинга состояния системы в реальном времени
- Управления модулями (запуск, остановка, конфигурация)
- Взаимодействия с системой через чат и терминал
- Настройки параметров и загрузки данных

### 1.2 Название и брендинг

- **Название:** NeuroGraph Dashboard (или просто Dashboard в UI)
- **Логотип:** 🧠 (emoji) или SVG иконка нейросети
- **Цветовая схема:** Тёмная тема по умолчанию, с возможностью переключения на светлую

### 1.3 Целевая аудитория

- Разработчики, работающие с NeuroGraph
- Администраторы системы
- Исследователи, экспериментирующие с когнитивной архитектурой

---

## 2. Технологический стек

### 2.1 Основные технологии

| Компонент | Технология | Версия | Назначение |
|-----------|------------|--------|------------|
| Framework | React | 18.x | UI библиотека |
| UI Kit | Ant Design Pro | 6.x | Готовые admin-компоненты |
| Language | TypeScript | 5.x | Типизация |
| State | Zustand | 4.x | Управление состоянием |
| Charts | Recharts | 2.x | Графики и диаграммы |
| Terminal | xterm.js | 5.x | Терминал в браузере |
| Build | Vite | 5.x | Сборка проекта |
| i18n | react-i18next | 14.x | Интернационализация |
| HTTP | axios | 1.x | API запросы |

### 2.2 Почему эти технологии

**Ant Design Pro:**
- Готовый ProLayout с sidebar, header, breadcrumbs
- ProTable — мощные таблицы с фильтрами, сортировкой, пагинацией
- ProForm — формы с валидацией
- Встроенная поддержка тем (dark/light)
- Отличная документация на китайском и английском

**Zustand:**
- Минималистичный (vs Redux)
- Отличная TypeScript поддержка
- Не требует провайдеров
- Легко интегрируется с React

**Vite:**
- Мгновенный HMR
- Быстрая сборка
- Простая конфигурация

### 2.3 Деплой

Dashboard встраивается в существующий FastAPI сервер:

```
src/api/
├── main.py              # FastAPI app
├── routers/             # API endpoints
└── static/              # ← Сюда собирается dashboard
    ├── index.html
    ├── assets/
    │   ├── index-xxx.js
    │   └── index-xxx.css
    └── locales/
        ├── ru.json
        └── en.json
```

FastAPI будет отдавать статику:
- `GET /` → `static/index.html`
- `GET /assets/*` → static files
- `GET /api/*` → API endpoints
- `WS /ws` → WebSocket

---

## 3. Архитектура приложения

### 3.1 Структура директорий

```
src/web/
├── package.json
├── pnpm-lock.yaml
├── vite.config.ts
├── tsconfig.json
├── index.html
│
├── public/
│   └── favicon.ico
│
├── locales/                    # i18n переводы
│   ├── ru/
│   │   └── translation.json
│   └── en/
│       └── translation.json
│
└── src/
    ├── main.tsx                # Entry point
    ├── App.tsx                 # Root component + providers
    ├── routes.tsx              # React Router конфигурация
    ├── i18n.ts                 # i18next конфигурация
    │
    ├── layouts/
    │   └── MainLayout.tsx      # ProLayout обёртка
    │
    ├── pages/                  # Страницы (по экранам)
    │   ├── Dashboard/
    │   ├── Modules/
    │   ├── Config/
    │   ├── Bootstrap/
    │   ├── Chat/
    │   ├── Terminal/
    │   └── Admin/
    │
    ├── components/             # Переиспользуемые компоненты
    │   ├── StatusBadge/
    │   ├── MetricCard/
    │   ├── LiveChart/
    │   └── ThemeToggle/
    │
    ├── stores/                 # Zustand stores
    │   ├── useSystemStore.ts
    │   ├── useModulesStore.ts
    │   ├── useChatStore.ts
    │   └── useSettingsStore.ts
    │
    ├── services/               # API и WebSocket
    │   ├── api.ts
    │   └── websocket.ts
    │
    ├── hooks/                  # Custom React hooks
    │   ├── useMetrics.ts
    │   ├── useWebSocket.ts
    │   └── useTheme.ts
    │
    ├── types/                  # TypeScript типы
    │   ├── api.ts
    │   ├── modules.ts
    │   └── metrics.ts
    │
    └── utils/                  # Утилиты
        ├── formatters.ts
        └── constants.ts
```

### 3.2 Роутинг

| Путь | Страница | Описание |
|------|----------|----------|
| `/` | Dashboard | Главная с метриками |
| `/modules` | Modules | Управление модулями |
| `/config` | Config | Настройки системы |
| `/bootstrap` | Bootstrap | Загрузка embeddings |
| `/chat` | Chat | Диалог с системой |
| `/terminal` | Terminal | Командная строка |
| `/admin` | Admin | CDNA и системные операции |

### 3.3 State Management

**useSystemStore** — системное состояние:
- `status`: 'running' | 'stopped' | 'error'
- `metrics`: { tokens, connections, queries, latency, ... }
- `version`: string
- `uptime`: number

**useModulesStore** — модули:
- `modules`: Module[]
- `loading`: boolean
- `startModule(id)`, `stopModule(id)`, `restartModule(id)`

**useChatStore** — чат:
- `messages`: Message[]
- `isTyping`: boolean
- `sendMessage(text)`, `clearHistory()`

**useSettingsStore** — настройки UI:
- `theme`: 'dark' | 'light'
- `language`: 'ru' | 'en'
- `sidebarCollapsed`: boolean

---

## 4. Экраны и компоненты

### 4.1 Dashboard (Главная)

**Назначение:** Обзор состояния системы, ключевые метрики, последняя активность.

**Layout:**
```
┌─────────────────────────────────────────────────────────────┐
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │  Статус   │ │  Токены   │ │   Связи   │ │ Запросы/ч │   │
│  │ 🟢 Работа │ │  50,000   │ │ 1,247,832 │ │  12,847   │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
│                                                             │
│  ┌────────────────────────┐ ┌────────────────────────────┐ │
│  │   Системные метрики    │ │    Внутренние метрики      │ │
│  │ CPU    [████████░░] 78%│ │ События/сек:  14,234       │ │
│  │ Memory [██████░░░░] 62%│ │ Ср. задержка: 14.4 мкс     │ │
│  │ Disk   [███░░░░░░░] 34%│ │ Fast Path:    56.3%        │ │
│  └────────────────────────┘ │ Cache Hit:    69.5%        │ │
│                             └────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────┐   │
│  │            Последняя активность                     │   │
│  │ 12:34:56  Запрос: "кошка" → 10 результатов (14мс)  │   │
│  │ 12:34:52  Feedback: positive на #12846             │   │
│  │ 12:34:48  Запрос: "программирование" → 8 результ.  │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Компоненты:**

1. **MetricCards** — 4 карточки сверху
   - Статус системы (с цветовым индикатором)
   - Количество токенов
   - Количество связей
   - Запросов в час
   - Данные обновляются через WebSocket

2. **SystemMetrics** — системные ресурсы
   - CPU, Memory, Disk (progress bars)
   - Данные из `/api/v1/metrics`

3. **InternalMetrics** — внутренние метрики NeuroGraph
   - Events/sec, Latency, Fast Path %, Cache Hit %
   - Данные из `/api/v1/metrics`

4. **RecentActivity** — таблица последних событий
   - Время, тип, описание, результат
   - Auto-scroll к новым записям
   - Данные через WebSocket (subscribe: "activity")

**API:**
- `GET /api/v1/health` — статус системы
- `GET /api/v1/metrics` — все метрики
- `GET /api/v1/tokens/count` — количество токенов
- `WS subscribe: "metrics"` — real-time обновления
- `WS subscribe: "activity"` — поток активности

---

### 4.2 Modules (Модули)

**Назначение:** Просмотр и управление модулями системы.

**Layout:**
```
┌─────────────────────────────────────────────────────────────┐
│  [+ Добавить модуль]                    🔍 Поиск...         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 🟢 TokenManager                          v2.0.0     │   │
│  │    Токенов: 50,000 | Операций: 12,847/сек          │   │
│  │    [Подробнее] [Перезапустить] [Остановить]        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 🟢 IntuitionEngine                       v3.0.0     │   │
│  │    Fast Path: 56.3% | Задержка: 69.5 мкс           │   │
│  │    [Подробнее] [Перезапустить] [Остановить]        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 🟢 SignalSystem                          v1.1.0     │   │
│  │    События: 304,553/сек | Подписок: 24             │   │
│  │    [Подробнее] [Перезапустить] [Остановить]        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 🔴 Gateway                               v2.0.0     │   │
│  │    Статус: Остановлен                              │   │
│  │    [Подробнее] [Запустить]                         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Компоненты:**

1. **ModuleList** — список всех модулей
   - Карточки или ProTable
   - Фильтрация по статусу
   - Поиск по названию

2. **ModuleCard** — карточка модуля
   - Название, версия
   - Статус (🟢 Running, 🟡 Starting, 🔴 Stopped, ⚠️ Error)
   - Ключевые метрики модуля
   - Кнопки действий

3. **ModuleDetailModal** — детали модуля (открывается по клику "Подробнее")
   - Полная конфигурация
   - Логи модуля
   - Графики метрик
   - История перезапусков

**Модули системы:**
- TokenManager — управление токенами
- ConnectionManager — управление связями
- IntuitionEngine — интуитивный движок
- SignalSystem — обработка сигналов
- Gateway — входные сенсоры
- ActionController — выходные действия
- Guardian — валидация и защита
- Grid — пространственный индекс

**API:**
- `GET /api/v1/modules` — список модулей
- `GET /api/v1/modules/{id}` — детали модуля
- `POST /api/v1/modules/{id}/start` — запуск
- `POST /api/v1/modules/{id}/stop` — остановка
- `POST /api/v1/modules/{id}/restart` — перезапуск

---

### 4.3 Config (Настройки)

**Назначение:** Редактирование конфигурации системы.

**Layout:**
```
┌─────────────────────────────────────────────────────────────┐
│  [Runtime] [Gateway] [Intuition] [Signal] [Guardian]        │
│  ───────────────────────────────────────────────────────── │
│                                                             │
│  Настройки Runtime                                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │ Максимум токенов     [      1,000,000      ]       │   │
│  │ Максимум связей      [     10,000,000      ]       │   │
│  │ Уровень логов        [ INFO            ▼ ]         │   │
│  │ Автосохранение       [✓] каждые [ 60 ] сек         │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ⚠️ Некоторые изменения требуют перезапуска                │
│                                                             │
│  [Сбросить]                        [Отмена] [Применить]    │
└─────────────────────────────────────────────────────────────┘
```

**Компоненты:**

1. **ConfigTabs** — табы по секциям
   - Runtime — основные параметры
   - Gateway — настройки сенсоров и энкодеров
   - Intuition — параметры IntuitionEngine
   - Signal — настройки SignalSystem
   - Guardian — правила валидации

2. **ConfigForm** — форма с ProForm
   - Группировка полей
   - Валидация на клиенте
   - Подсказки (tooltips) для каждого поля
   - Индикация изменённых полей

3. **ConfigActions** — кнопки действий
   - Сбросить к значениям по умолчанию
   - Отмена (вернуть к сохранённым)
   - Применить (с подтверждением если требует рестарт)

**API:**
- `GET /api/v1/config` — текущая конфигурация
- `GET /api/v1/config/{section}` — секция конфигурации
- `PUT /api/v1/config/{section}` — обновить секцию
- `POST /api/v1/config/reset` — сбросить к defaults

---

### 4.4 Bootstrap (Загрузка данных)

**Назначение:** Загрузка word embeddings и начальных данных.

**Layout:**
```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│  Загрузка Word Embeddings                                   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │     📁 Перетащите файл сюда                        │   │
│  │        или нажмите для выбора                      │   │
│  │                                                     │   │
│  │     Поддерживаемые форматы:                        │   │
│  │     • GloVe (.txt)                                 │   │
│  │     • Word2Vec (.bin, .txt)                        │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Формат:  (•) GloVe  ( ) Word2Vec                          │
│  Лимит:   [     50,000     ] слов (0 = все)                │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  Текущее состояние:                                         │
│  • Загружено: 50,000 концептов                             │
│  • Размерность: 50                                         │
│  • Файл: glove.6B.50d.txt                                  │
│  • Загружен: 2024-12-30 12:34:56                           │
│                                                             │
│                                      [Очистить] [Загрузить] │
└─────────────────────────────────────────────────────────────┘
```

**Компоненты:**

1. **UploadArea** — зона загрузки (Ant Design Upload.Dragger)
   - Drag & drop
   - Выбор файла
   - Валидация формата

2. **UploadOptions** — опции загрузки
   - Формат (GloVe / Word2Vec)
   - Лимит слов

3. **UploadProgress** — прогресс загрузки
   - Progress bar
   - Текущий статус
   - Время до завершения

4. **BootstrapStatus** — текущее состояние
   - Сколько загружено
   - Размерность
   - Когда загружено
   - Кнопка очистки

**API:**
- `POST /api/v1/bootstrap/upload` — загрузка файла (multipart)
- `GET /api/v1/bootstrap/status` — статус загрузки
- `DELETE /api/v1/bootstrap` — очистка

---

### 4.5 Chat (Диалог)

**Назначение:** Интерактивное взаимодействие с системой через текстовый интерфейс.

**Layout:**
```
┌─────────────────────────────────────────────────────────────┐
│  💬 Диалог с NeuroGraph                    [Очистить]       │
│  ───────────────────────────────────────────────────────── │
│                                                             │
│                           Привет! Я NeuroGraph.             │
│                           Задайте вопрос или команду.       │
│                                                 🧠 12:30    │
│                                                             │
│  Найди похожие слова на "кошка"                            │
│  👤 12:31                                                   │
│                                                             │
│                           Найдено 10 похожих концептов:     │
│                           1. котёнок (0.89)                 │
│                           2. собака (0.85)                  │
│                           3. животное (0.82)                │
│                           ...                               │
│                           Время: 14.2 мс                    │
│                                                 🧠 12:31    │
│                                                             │
│  ───────────────────────────────────────────────────────── │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Введите сообщение...                           [➤] │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Компоненты:**

1. **ChatHeader** — заголовок
   - Название
   - Кнопка очистки истории

2. **MessageList** — список сообщений
   - Сообщения пользователя (справа, синий фон)
   - Сообщения системы (слева, серый фон)
   - Timestamp для каждого
   - Auto-scroll к новым

3. **MessageBubble** — пузырёк сообщения
   - Аватар (👤 или 🧠)
   - Текст сообщения
   - Время
   - Форматирование (markdown, code blocks)

4. **TypingIndicator** — индикатор "печатает..."
   - Три анимированные точки

5. **ChatInput** — поле ввода
   - Textarea (многострочный)
   - Кнопка отправки
   - Enter для отправки, Shift+Enter для новой строки

**Команды чата:**
- Обычный текст → семантический поиск
- `/status` → статус системы
- `/help` → список команд
- `/clear` → очистить историю
- `/modules` → список модулей

**API:**
- `POST /api/v1/chat/message` — отправка сообщения
- `GET /api/v1/chat/history` — история (опционально)
- `WS subscribe: "chat"` — real-time ответы

---

### 4.6 Terminal (Терминал)

**Назначение:** Полноценная командная строка для продвинутых операций.

**Layout:**
```
┌─────────────────────────────────────────────────────────────┐
│  💻 Терминал                                [Очистить]      │
│  ───────────────────────────────────────────────────────── │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ NeuroGraph Terminal v0.62.0                         │   │
│  │ Type 'help' for available commands.                 │   │
│  │                                                     │   │
│  │ > status                                            │   │
│  │ System: Running                                     │   │
│  │ Uptime: 2h 34m 12s                                  │   │
│  │ Tokens: 50,000                                      │   │
│  │ Connections: 1,247,832                              │   │
│  │                                                     │   │
│  │ > query "программирование" --limit 5               │   │
│  │ Results (5 of 847):                                 │   │
│  │   1. код (0.92)                                     │   │
│  │   2. разработка (0.89)                              │   │
│  │   3. алгоритм (0.87)                                │   │
│  │   4. компьютер (0.85)                               │   │
│  │   5. software (0.83)                                │   │
│  │ Time: 12.4ms                                        │   │
│  │                                                     │   │
│  │ > _                                                 │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Компоненты:**

1. **TerminalContainer** — контейнер xterm.js
   - Полноэкранный терминал
   - Тёмная тема
   - Моноширинный шрифт

2. **TerminalHeader** — заголовок
   - Название
   - Кнопка очистки

**Функциональность xterm.js:**
- История команд (↑/↓)
- Автодополнение (Tab)
- Copy/Paste
- Прокрутка
- Цветной вывод (ANSI codes)

**Доступные команды:**
- `help` — список команд
- `status` — статус системы
- `modules` — список модулей
- `query <text>` — семантический поиск
- `token create <params>` — создать токен
- `token get <id>` — получить токен
- `cdna show` — показать CDNA конфигурацию
- `cdna set <param> <value>` — изменить CDNA
- `clear` — очистить терминал
- `exit` — закрыть (переход на Dashboard)

**API:**
- `WS /ws/terminal` — bidirectional терминал
- Или через общий WebSocket с типом сообщения "terminal"

---

### 4.7 Admin (Администрирование)

**Назначение:** CDNA конфигурация и системные операции.

**Layout:**
```
┌─────────────────────────────────────────────────────────────┐
│  🔐 Администрирование                                       │
│  ───────────────────────────────────────────────────────── │
│                                                             │
│  CDNA Configuration                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Профиль: [ Balanced        ▼ ]                      │   │
│  │                                                     │   │
│  │ Dimension Scales:                                   │   │
│  │ Physical   [━━━━━━━━━━━━━━━━━━] 1.0                │   │
│  │ Sensory    [━━━━━━━━━━━━━━━━━━] 1.0                │   │
│  │ Motor      [━━━━━━━━━━━━━━━━━━] 1.0                │   │
│  │ Emotional  [━━━━━━━━━━━━━━━━━━] 1.0                │   │
│  │ Cognitive  [━━━━━━━━━━━━━━━━━━] 1.0                │   │
│  │ Social     [━━━━━━━━━━━━━━━━━━] 1.0                │   │
│  │ Temporal   [━━━━━━━━━━━━━━━━━━] 1.0                │   │
│  │ Abstract   [━━━━━━━━━━━━━━━━━━] 1.0                │   │
│  │                                                     │   │
│  │                               [Сбросить] [Применить]│   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  Системные операции                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ [Экспорт данных]  [Импорт данных]  [Создать backup] │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ⚠️ Опасная зона                                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ [Очистить все токены]  [Сбросить систему]          │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Компоненты:**

1. **CDNAEditor** — редактор CDNA
   - Выбор профиля (Balanced, Explorer, Focused, Creative)
   - Слайдеры для каждого измерения (0.0 — 2.0)
   - Визуализация (radar chart)
   - Кнопки сброса и применения

2. **SystemOperations** — системные операции
   - Экспорт данных (JSON, binary)
   - Импорт данных
   - Создание backup

3. **DangerZone** — опасные операции
   - Очистка токенов (с подтверждением)
   - Полный сброс системы (с двойным подтверждением)
   - Красная рамка для визуального предупреждения

**CDNA Profiles:**
- **Balanced** (0) — все весы = 1.0
- **Explorer** (1) — выше Abstract, Cognitive
- **Focused** (2) — выше Sensory, Motor
- **Creative** (3) — выше Emotional, Social

**API:**
- `GET /api/v1/cdna/config` — текущая конфигурация
- `PUT /api/v1/cdna/scales` — обновить весы
- `PUT /api/v1/cdna/profile` — установить профиль
- `POST /api/v1/admin/export` — экспорт
- `POST /api/v1/admin/import` — импорт
- `POST /api/v1/admin/backup` — backup
- `DELETE /api/v1/admin/tokens` — очистить токены
- `POST /api/v1/admin/reset` — полный сброс

---

## 5. Общие компоненты

### 5.1 MainLayout

Обёртка ProLayout с:
- **Header:**
  - Логотип и название слева
  - Поиск (опционально)
  - Уведомления
  - Переключатель темы
  - Переключатель языка
  - Аватар пользователя

- **Sidebar:**
  - Навигационное меню
  - Иконки для каждого пункта
  - Статус системы внизу (🟢 Running, версия)
  - Сворачивание

- **Footer:** (опционально)
  - Copyright
  - Ссылки на документацию

### 5.2 StatusBadge

Индикатор статуса:
- 🟢 Running / Работает
- 🟡 Starting / Запускается
- 🔴 Stopped / Остановлен
- ⚠️ Error / Ошибка
- 🔄 Restarting / Перезапуск

### 5.3 MetricCard

Карточка метрики:
- Заголовок
- Значение (большим шрифтом)
- Тренд (↑ +5% или ↓ -3%)
- Иконка

### 5.4 LiveChart

График в реальном времени:
- Line chart для метрик
- Auto-update через WebSocket
- Последние N точек
- Tooltip при наведении

### 5.5 ThemeToggle

Переключатель темы:
- 🌙 / ☀️ иконки
- Сохранение в localStorage
- CSS variables для цветов

### 5.6 LanguageSwitch

Переключатель языка:
- 🇷🇺 RU / 🇬🇧 EN
- Сохранение в localStorage
- Мгновенное переключение (react-i18next)

---

## 6. Интернационализация (i18n)

### 6.1 Структура

```
locales/
├── ru/
│   └── translation.json
└── en/
    └── translation.json
```

### 6.2 Ключи (примеры)

```json
{
  "common": {
    "status": "Статус",
    "actions": "Действия",
    "save": "Сохранить",
    "cancel": "Отмена",
    "apply": "Применить",
    "reset": "Сбросить",
    "loading": "Загрузка...",
    "error": "Ошибка",
    "success": "Успешно"
  },
  "nav": {
    "dashboard": "Панель",
    "modules": "Модули",
    "config": "Настройки",
    "bootstrap": "Загрузка",
    "chat": "Диалог",
    "terminal": "Терминал",
    "admin": "Админ"
  },
  "dashboard": {
    "title": "Обзор системы",
    "status": "Статус системы",
    "tokens": "Токенов",
    "connections": "Связей",
    "queries": "Запросов/час",
    "systemMetrics": "Системные метрики",
    "internalMetrics": "Внутренние метрики",
    "recentActivity": "Последняя активность"
  },
  "modules": {
    "title": "Управление модулями",
    "start": "Запустить",
    "stop": "Остановить",
    "restart": "Перезапустить",
    "details": "Подробнее"
  },
  "status": {
    "running": "Работает",
    "stopped": "Остановлен",
    "starting": "Запускается",
    "error": "Ошибка"
  }
}
```

### 6.3 Использование

```typescript
import { useTranslation } from 'react-i18next';

function Component() {
  const { t } = useTranslation();
  return <span>{t('dashboard.title')}</span>;
}
```

---

## 7. WebSocket протокол

### 7.1 Подключение

```
WS /ws
```

### 7.2 Формат сообщений

**Client → Server:**
```json
{
  "type": "subscribe",
  "channel": "metrics"
}

{
  "type": "unsubscribe", 
  "channel": "metrics"
}

{
  "type": "chat",
  "payload": {
    "message": "Hello"
  }
}

{
  "type": "terminal",
  "payload": {
    "command": "status"
  }
}
```

**Server → Client:**
```json
{
  "type": "metrics",
  "payload": {
    "tokens": 50000,
    "connections": 1247832,
    "events_per_sec": 14234,
    "avg_latency_us": 14.4
  }
}

{
  "type": "activity",
  "payload": {
    "time": "2024-12-30T12:34:56Z",
    "event": "query",
    "details": "кошка → 10 results",
    "duration_ms": 14.2
  }
}

{
  "type": "chat",
  "payload": {
    "message": "Найдено 10 результатов...",
    "timestamp": "2024-12-30T12:34:56Z"
  }
}

{
  "type": "terminal",
  "payload": {
    "output": "System: Running\nUptime: 2h 34m 12s\n"
  }
}
```

### 7.3 Каналы подписки

- `metrics` — метрики системы (каждые 1-2 сек)
- `activity` — поток активности (по событию)
- `modules` — изменения статуса модулей
- `chat` — ответы чата
- `terminal` — вывод терминала

---

## 8. Темы оформления

### 8.1 Тёмная тема (по умолчанию)

```css
:root[data-theme="dark"] {
  --bg-primary: #141414;
  --bg-secondary: #1f1f1f;
  --bg-tertiary: #262626;
  --text-primary: #ffffff;
  --text-secondary: #a0a0a0;
  --accent: #1890ff;
  --success: #52c41a;
  --warning: #faad14;
  --error: #ff4d4f;
  --border: #434343;
}
```

### 8.2 Светлая тема

```css
:root[data-theme="light"] {
  --bg-primary: #ffffff;
  --bg-secondary: #fafafa;
  --bg-tertiary: #f5f5f5;
  --text-primary: #000000;
  --text-secondary: #595959;
  --accent: #1890ff;
  --success: #52c41a;
  --warning: #faad14;
  --error: #ff4d4f;
  --border: #d9d9d9;
}
```

### 8.3 Ant Design Pro

Использовать встроенный механизм тем:
- `algorithm: theme.darkAlgorithm` для тёмной
- `algorithm: theme.defaultAlgorithm` для светлой

---

## 9. План реализации

### Phase 1: Project Setup (1 день)

**Задачи:**
- [ ] Создать Vite проект с React + TypeScript
- [ ] Установить и настроить Ant Design Pro
- [ ] Настроить Zustand stores (пустые)
- [ ] Настроить react-i18next с базовыми переводами
- [ ] Создать MainLayout с ProLayout
- [ ] Настроить роутинг (react-router-dom v6)
- [ ] Создать API service (axios instance)
- [ ] Создать WebSocket service (базовый)
- [ ] Настроить Vite для сборки в `src/api/static/`

**Файлы:**
- `package.json`, `vite.config.ts`, `tsconfig.json`
- `src/main.tsx`, `src/App.tsx`, `src/routes.tsx`
- `src/i18n.ts`
- `src/layouts/MainLayout.tsx`
- `src/services/api.ts`, `src/services/websocket.ts`
- `src/stores/*.ts` (заглушки)
- `locales/ru/translation.json`, `locales/en/translation.json`

### Phase 2: Dashboard (1.5 дня)

**Задачи:**
- [ ] Создать страницу Dashboard
- [ ] MetricCards компонент (4 карточки)
- [ ] SystemMetrics компонент (CPU/Memory/Disk)
- [ ] InternalMetrics компонент
- [ ] RecentActivity таблица
- [ ] useSystemStore с реальными данными
- [ ] WebSocket подписка на metrics
- [ ] WebSocket подписка на activity

**Файлы:**
- `src/pages/Dashboard/index.tsx`
- `src/pages/Dashboard/MetricCards.tsx`
- `src/pages/Dashboard/SystemMetrics.tsx`
- `src/pages/Dashboard/InternalMetrics.tsx`
- `src/pages/Dashboard/RecentActivity.tsx`
- `src/components/MetricCard/index.tsx`
- `src/stores/useSystemStore.ts`
- `src/hooks/useMetrics.ts`

### Phase 3: Modules (1 день)

**Задачи:**
- [ ] Создать страницу Modules
- [ ] ModuleCard компонент
- [ ] ModuleDetailModal компонент
- [ ] useModulesStore с CRUD
- [ ] Actions: start/stop/restart

**Файлы:**
- `src/pages/Modules/index.tsx`
- `src/pages/Modules/ModuleCard.tsx`
- `src/pages/Modules/ModuleDetailModal.tsx`
- `src/stores/useModulesStore.ts`

### Phase 4: Config (1 день)

**Задачи:**
- [ ] Создать страницу Config
- [ ] Табы для секций
- [ ] ProForm для каждой секции
- [ ] Валидация
- [ ] Apply/Reset логика

**Файлы:**
- `src/pages/Config/index.tsx`
- `src/pages/Config/RuntimeConfig.tsx`
- `src/pages/Config/GatewayConfig.tsx`
- ... (по секциям)

### Phase 5: Bootstrap (0.5 дня)

**Задачи:**
- [ ] Создать страницу Bootstrap
- [ ] Upload.Dragger компонент
- [ ] Опции загрузки
- [ ] Progress bar
- [ ] Статус отображение

**Файлы:**
- `src/pages/Bootstrap/index.tsx`
- `src/pages/Bootstrap/UploadArea.tsx`
- `src/pages/Bootstrap/BootstrapStatus.tsx`

### Phase 6: Chat (1.5 дня)

**Задачи:**
- [ ] Создать страницу Chat
- [ ] MessageList компонент
- [ ] MessageBubble компонент
- [ ] ChatInput компонент
- [ ] TypingIndicator
- [ ] useChatStore
- [ ] WebSocket интеграция

**Файлы:**
- `src/pages/Chat/index.tsx`
- `src/pages/Chat/MessageList.tsx`
- `src/pages/Chat/MessageBubble.tsx`
- `src/pages/Chat/ChatInput.tsx`
- `src/stores/useChatStore.ts`

### Phase 7: Terminal (1 день)

**Задачи:**
- [ ] Создать страницу Terminal
- [ ] Интегрировать xterm.js
- [ ] WebSocket для ввода/вывода
- [ ] История команд
- [ ] Цветной вывод

**Файлы:**
- `src/pages/Terminal/index.tsx`
- `src/hooks/useTerminal.ts`

### Phase 8: Admin (0.5 дня)

**Задачи:**
- [ ] Создать страницу Admin
- [ ] CDNAEditor с слайдерами
- [ ] Radar chart визуализация
- [ ] SystemOperations кнопки
- [ ] DangerZone с подтверждениями

**Файлы:**
- `src/pages/Admin/index.tsx`
- `src/pages/Admin/CDNAEditor.tsx`
- `src/pages/Admin/SystemOperations.tsx`
- `src/pages/Admin/DangerZone.tsx`

### Phase 9: Polish (1 день)

**Задачи:**
- [ ] ThemeToggle компонент
- [ ] LanguageSwitch компонент
- [ ] Responsive адаптация
- [ ] Error boundaries
- [ ] Loading states везде
- [ ] 404 страница
- [ ] Финальная проверка i18n
- [ ] Production build
- [ ] Интеграция с FastAPI (статика)

**Файлы:**
- `src/components/ThemeToggle/index.tsx`
- `src/components/LanguageSwitch/index.tsx`
- `src/pages/NotFound.tsx`
- Обновление `src/api/main.py` для статики

---

## 10. Интеграция с FastAPI

### 10.1 Сборка

```bash
cd src/web
pnpm build
# Результат в dist/

# Копирование в FastAPI
cp -r dist/* ../api/static/
```

### 10.2 FastAPI конфигурация

```python
# src/api/main.py
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse

app = FastAPI()

# API routes
app.include_router(tokens_router, prefix="/api/v1")
app.include_router(cdna_router, prefix="/api/v1")
# ... другие роутеры

# Static files (после API роутов!)
app.mount("/assets", StaticFiles(directory="static/assets"), name="assets")

# SPA fallback — все остальные пути → index.html
@app.get("/{full_path:path}")
async def serve_spa(full_path: str):
    return FileResponse("static/index.html")
```

### 10.3 Vite конфигурация

```typescript
// vite.config.ts
export default defineConfig({
  base: '/',
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
  },
  // ...
})
```

---

## 11. Тестирование

### 11.1 Что тестировать

- **Unit:** stores, utils, formatters
- **Integration:** API calls, WebSocket
- **E2E:** основные user flows (опционально)

### 11.2 Минимальный набор

- Dashboard загружается и показывает метрики
- Modules отображают список и кнопки работают
- Chat отправляет и получает сообщения
- Theme toggle работает
- Language switch работает

---

## 12. Deliverables

По завершении v0.62.0:

- [ ] Web Dashboard встроен в FastAPI
- [ ] 7 экранов работают
- [ ] Real-time обновления через WebSocket
- [ ] Тёмная/светлая тема
- [ ] Русский/английский язык
- [ ] Responsive дизайн
- [ ] Production build < 500KB gzipped
- [ ] CHANGELOG_v0.62.0.md
- [ ] Обновлённый README

---

## 13. Важные замечания

### 13.1 Для разработчика

1. **Не изобретать велосипед** — использовать готовые компоненты Ant Design Pro
2. **TypeScript строго** — никаких `any`, все типы определены
3. **i18n с первого дня** — все строки через `t()`
4. **Mobile-first** — сначала мобильная версия, потом desktop

### 13.2 Приоритеты

Если не хватает времени, сначала делать:
1. Dashboard (критично)
2. Chat (критично)
3. Modules (важно)
4. Terminal (важно)
5. Config (можно упростить)
6. Bootstrap (можно упростить)
7. Admin (можно упростить)

### 13.3 Известные API endpoints

Уже реализованы (использовать существующие):
- `GET /api/v1/health`
- `GET /api/v1/metrics`
- `GET /api/v1/tokens`, `POST`, `PUT`, `DELETE`
- `GET /api/v1/grid/info`, `neighbors`, `range`
- `GET /api/v1/cdna/config`, `PUT scales`, `PUT profile`
- `WS /ws` с подписками

---

**Конец спецификации.**

*Готово к реализации. Удачи, Sonnet!* 🚀
