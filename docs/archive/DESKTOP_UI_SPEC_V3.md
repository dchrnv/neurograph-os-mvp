# NeuroGraph Desktop UI v3.0 — Спецификация

**Версия:** 3.0.0
**Статус:** DRAFT
**Framework:** iced (Rust)
**Целевая версия системы:** v0.46.0
**Дата:** Январь 2025

---

## 1. Общая концепция

### Что это

Операционная панель для NeuroGraph — полноценный desktop-инструмент для мониторинга, управления и взаимодействия с когнитивной архитектурой.

### Для кого

- Разработчики, работающие с системой
- DevOps для мониторинга в production
- Исследователи для экспериментов

### Ключевые принципы

- **Ясность** — информация читается с первого взгляда, без визуального шума
- **Фокус** — каждый экран решает одну задачу хорошо
- **Отзывчивость** — данные обновляются в реальном времени, UI не тормозит
- **Клавиатура** — всё доступно без мыши для power users

---

## 2. Визуальный стиль: Terminal Modern

### Философия

Вдохновение: Linear, Raycast, GitHub Dark, Warp Terminal. Серьёзный профессиональный инструмент, но не скучный. Чистые линии, минимум декора, фокус на контенте.

### Цветовая палитра

**Фоны:**

- Основной фон приложения: `#0d1117` (глубокий тёмно-синий)
- Фон карточек и панелей: `#161b22` (чуть светлее)
- Фон при наведении: `#21262d`
- Фон активного элемента: `#30363d`

**Текст:**

- Основной: `#c9d1d9` (светло-серый, хорошо читается)
- Вторичный: `#8b949e` (для подписей, hints)
- Приглушённый: `#484f58` (placeholder, disabled)
- Ссылки: `#58a6ff` (голубой акцент)

**Акцент:**

- Основной: `#58a6ff` (мягкий голубой — кнопки, активные элементы)
- При наведении: `#79b8ff`

**Статусы:**

- Success: `#3fb950` (зелёный)
- Warning: `#d29922` (жёлто-оранжевый)
- Error: `#f85149` (красный)
- Info: `#58a6ff` (голубой)

**Границы:**

- Обычная: `#30363d`
- При фокусе: `#58a6ff`
- Тонкая (разделители): `#21262d`

### Типографика

**Шрифты:**

- UI элементы: Inter (или системный sans-serif)
- Код, данные, терминал: JetBrains Mono (или системный monospace)

**Размеры:**

- Мелкие подписи: 11px
- Вторичный текст: 12px
- Основной текст: 14px
- Заголовки карточек: 16px
- Заголовки секций: 20px
- Заголовки страниц: 24px

### Отступы и скругления

- Базовый отступ внутри элементов: 12-16px
- Между элементами: 8-16px
- Скругление карточек: 8px
- Скругление кнопок: 6px
- Скругление input: 6px

### Тени

Минимально. Предпочтение границам. Если нужна тень — очень subtle, только для модальных окон.

---

## 3. Структура приложения

### Поток пользователя

```
Запуск → Экран авторизации (PIN) → Главное окно
```

### Главное окно: Layout

```
┌────────────────────────────────────────────────────────────────┐
│  Header: Logo + Название экрана + Status indicators            │
├──────────┬─────────────────────────────────────────────────────┤
│          │                                                     │
│ Sidebar  │              Основной контент                       │
│ (nav)    │              (зависит от выбранного экрана)         │
│          │                                                     │
│ ──────── │                                                     │
│ Dashboard│                                                     │
│ Chat     │                                                     │
│ Modules  │                                                     │
│ Logs     │                                                     │
│ Settings │                                                     │
│ Integr.  │                                                     │
│          │                                                     │
├──────────┴─────────────────────────────────────────────────────┤
│  Status Bar: Tokens | Connections | Memory | Uptime            │
└────────────────────────────────────────────────────────────────┘
```

### Экраны (вкладки)

1. **Dashboard** — обзор системы, ключевые метрики, quick actions
2. **Chat** — диалоговый интерфейс с переключением Chat/Terminal
3. **Modules** — управление модулями системы
4. **Logs** — просмотр логов с фильтрацией
5. **Settings** — все настройки системы
6. **Integrations** — подключение внешних модулей/нейронок/библиотек

---

## 4. Экран авторизации

### Назначение

Простая защита от случайного доступа. Не security-critical, просто PIN.

### Внешний вид

Центрированная карточка на тёмном фоне:

- Логотип "NEUROGRAPH" сверху
- Подпись "Enter PIN"
- Поле ввода PIN (отображается точками: ● ● ● ○)
- Кнопка "Unlock"
- Версия приложения внизу мелким шрифтом

### Поведение

- PIN: 4-6 цифр
- Ввод с клавиатуры, Enter для подтверждения
- При ошибке: лёгкая анимация "shake" + сообщение
- После 3 неудачных попыток: блокировка на 30 секунд
- Первый запуск: режим "Set new PIN" вместо "Enter PIN"

### Хранение

PIN хранится локально в зашифрованном виде (bcrypt hash в конфиге).

---

## 5. Header (верхняя панель)

### Содержимое

Слева направо:

1. **Логотип** — иконка + "NeuroGraph" (кликабельно → Dashboard)
2. **Breadcrumb** — название текущего экрана
3. **Spacer** — растягивается
4. **Status indicators** — маленькие индикаторы:
   - Точка статуса системы (зелёная/жёлтая/красная)
   - Иконка сети (connected/disconnected)
5. **User menu** — иконка пользователя, dropdown с "Lock" и "About"

### Размеры

- Высота: 48-56px
- Отступы по краям: 16px

---

## 6. Sidebar (левая панель навигации)

### Внешний вид

Фиксированная ширина, всегда видна. Тёмный фон (`#161b22`), чуть светлее основного.

### Элементы навигации

Вертикальный список:

| Иконка | Название | Горячая клавиша |
| ------------ | ---------------- | ----------------------------- |
| □           | Dashboard        | Ctrl+1                        |
| 💬           | Chat             | Ctrl+2                        |
| ◧           | Modules          | Ctrl+3                        |
| ☰           | Logs             | Ctrl+4                        |
| ⚙           | Settings         | Ctrl+5                        |
| ⊕           | Integrations     | Ctrl+6                        |

### Состояния кнопок

- **Default**: иконка + текст, обычный цвет
- **Hover**: фон `#21262d`
- **Active**: фон `#30363d`, левая граница акцентным цветом

---

## 7. Status Bar (нижняя панель)

### Содержимое

Слева направо, разделённые вертикальными линиями:

- **Tokens**: "1,234,567 tokens"
- **Connections**: "523,891 connections"
- **Memory**: "847 MB / 2 GB"
- **CPU**: "12%"
- **Uptime**: "3d 14h 22m"
- **API**: "REST: OK | WS: 3 clients"

### Стиль

- Высота: 24-28px
- Фон: `#161b22`
- Текст названий: `#8b949e`
- Значения: `#c9d1d9`
- Шрифт чисел: monospace

---

## 8. Dashboard

### Назначение

Главный экран — обзор состояния системы "одним взглядом".

### Секции

**Метрики (верхний ряд карточек):**

- TOKENS — количество + тренд (/min)
- CONNECTIONS — количество + тренд
- MEMORY — использование в MB и %
- THROUGHPUT — tok/sec + мини-график
- LATENCY — p99 в ms + мини-график
- UPTIME — время работы + % доступности

**Модули (средняя секция):**

Компактный grid всех модулей с индикаторами:

- ● Зелёный = Running
- ● Жёлтый = Starting/Stopping
- ● Красный = Error
- ○ Серый = Stopped

Клик по модулю → переход на Modules.

**Quick Actions (нижняя секция):**

Горизонтальный ряд кнопок:

- New Chat
- View Logs
- Restart All (с подтверждением)
- Export Config

---

## 9. Chat / Terminal

### Концепция

Два режима в одном экране с переключателем табами.

### Режим Chat

- Диалоговый интерфейс
- Сообщения пользователя и системы
- Markdown форматирование в ответах
- История сохраняется между сессиями

### Режим Terminal

- Командный интерфейс с prompt `neurograph>`
- Моноширинный шрифт
- Команды: `status`, `help`, `modules list`, `module restart <n>`, etc.
- Metadata для каждой команды (время выполнения)
- История команд по стрелке вверх

### Управление

- Табы переключения Chat/Terminal вверху
- Кнопка Clear — очистить историю
- Кнопка Export — экспорт в файл
- Enter = отправить
- Shift+Enter = новая строка (Chat)
- Стрелка вверх = предыдущая команда (Terminal)

---

## 10. Modules

### Назначение

Полное управление модулями: статус, start/stop/restart, конфигурация.

### Layout

Две колонки:

- **Левая** — список всех модулей с индикаторами статуса
- **Правая** — детали выбранного модуля

### Детали модуля

**Информация:**

- Название
- Статус (badge)
- Uptime
- Версия

**Actions:**

- Start / Stop
- Restart
- Logs (переход с фильтром)

**Configuration:**

- Отображение параметров
- Кнопка Edit Config → модальное окно редактирования

### Список модулей системы

- Gateway
- ActionController
- IntuitionEngine
- Guardian
- ExperienceStream
- CuriosityDrive
- Bootstrap Library
- ADNA
- CDNA
- FeedbackProcessor
- WAL
- Metrics
- BlackBox

---

## 11. Logs

### Назначение

Просмотр логов в реальном времени с фильтрацией.

### Фильтры

- **Level**: All / Error / Warn / Info / Debug / Trace
- **Module**: All / конкретный модуль
- **Search**: текстовый поиск

### Опции

- Auto-scroll (checkbox)
- Pause/Resume
- Clear
- Export

### Формат записи

```
14:32:01.234  INFO   Gateway      Request received
```

- Timestamp: моноширинный, приглушённый
- Level: цветной badge
- Module: моноширинный
- Message: основной текст

### Клик по записи

Раскрывает детали: stack trace, context, metadata.

---

## 12. Settings

### Табы

**General:**

- Data directory
- Auto-start
- Check for updates

**Appearance:**

- Theme (Dark)
- Font size
- Show status bar

**Modules:**

- Default configurations
- Auto-restart on failure
- Resource limits

**Network:**

- REST API port
- WebSocket port
- CORS settings
- API authentication

**Security:**

- Change PIN
- Session timeout
- Export/Import config

### Кнопки

- Reset to Defaults
- Save Changes (активна при изменениях)

---

## 13. Integrations

### Назначение

Подключение внешних модулей, нейронных сетей, библиотек.

### Layout

**Установленные интеграции:**

Список карточек:

- Иконка типа
- Название
- Статус (Connected / Error / Loading)
- Ключевая метрика
- Кнопки: Configure, Remove

**Доступные интеграции:**

Grid карточек для установки новых.

### Типы интеграций

**Neural Networks / LLMs:**

- OpenAI, Claude, Local LLMs, Hugging Face

**Embeddings:**

- GloVe, Word2Vec, FastText, Sentence Transformers

**Knowledge Bases:**

- Wikipedia API, Vector databases

**Tools:**

- Web search, Code execution

### Добавление

Кнопка "+ Add New" → модальное окно:

1. Выбор типа
2. Ввод параметров (API key, endpoint)
3. Тест соединения
4. Сохранение

---

## 14. Модальные окна

### Общий стиль

- Затемнение фона
- Карточка по центру
- Заголовок + кнопка закрытия (×)
- Контент
- Кнопки действий внизу

### Типы

- **Confirmation**: вопрос + Cancel/Confirm
- **Form**: поля + Cancel/Save
- **Alert**: сообщение + OK
- **Info**: детали + Close

---

## 15. Toast уведомления

### Позиция

Правый нижний угол, стек снизу вверх.

### Типы

- Success — зелёная полоса
- Error — красная
- Warning — жёлтая
- Info — голубая

### Поведение

- Автоматически исчезают через 5 секунд
- Можно закрыть вручную
- При hover таймер останавливается

---

## 16. Горячие клавиши

### Глобальные

| Клавиша | Действие                           |
| -------------- | ------------------------------------------ |
| Ctrl+1...6     | Переключение экранов    |
| Ctrl+L         | Lock (на экран PIN)                 |
| Ctrl+,         | Settings                                   |
| Escape         | Закрыть модальное окно |
| F5             | Refresh                                    |

### Chat/Terminal

| Клавиша | Действие                       |
| -------------- | -------------------------------------- |
| Enter          | Отправить                     |
| Shift+Enter    | Новая строка (Chat)         |
| ↑/↓          | История команд (Terminal) |

### Logs

| Клавиша | Действие      |
| -------------- | --------------------- |
| Space          | Pause/Resume          |
| Ctrl+F         | Focus на поиск |

---

## 17. FFI интеграция с Core

### Принцип

Прямые вызовы neurograph-core через FFI. Никакого HTTP — микросекундные задержки.

### CoreBridge методы

- `status()` → SystemStatus
- `query(text)` → QueryResponse
- `execute_command(cmd)` → CommandOutput
- `get_modules()` → Vec`<ModuleInfo>`
- `module_action(id, action)` → Result
- `get_metrics()` → SystemMetrics
- `get_logs(filter)` → Vec`<LogEntry>`
- `get_config()` → Config
- `update_config(changes)` → Result
- `get_integrations()` → Vec`<Integration>`

### Subscriptions

- `subscribe_metrics(interval)` → Stream
- `subscribe_logs(filter)` → Stream
- `subscribe_module_status()` → Stream

---

## 18. Responsive поведение

### Минимальный размер

- Ширина: 1024px
- Высота: 768px

### При уменьшении

1. Сжимается контент
2. Sidebar остаётся
3. При очень узком — sidebar в иконки

---

## 19. Анимации

Минимум, только функциональные:

- Fade in/out — 100-150ms
- Slide — toast, sidebar (150-200ms)
- Shake — ошибка PIN (300ms)
- Pulse — статус Starting
- Spinner — загрузка

---

## 20. Состояния

### Загрузка

- Skeleton для карточек (серые пульсирующие блоки)
- Spinner для действий
- Progress bar для длительных операций

### Пустые списки

- Иконка
- Текст "No logs found"
- Подсказка что делать

### Ошибки

- Inline под полями ввода
- Красная рамка карточки для модулей с ошибкой
- Full-screen если core недоступен

---

## 21. План реализации

### Phase 1: Foundation (3-4 дня)

- Структура проекта
- Theme system
- Базовые компоненты
- Экран авторизации
- Main layout
- Routing

### Phase 2: Dashboard (2-3 дня)

- Карточки метрик
- Секция модулей
- Quick actions
- FFI bridge
- Real-time subscriptions

### Phase 3: Chat/Terminal (2-3 дня)

- Оба режима
- История
- Интеграция с Gateway

### Phase 4: Modules (2-3 дня)

- Список и детали
- Actions
- Конфигурация

### Phase 5: Logs (1-2 дня)

- Таблица
- Фильтрация
- Streaming

### Phase 6: Settings (1-2 дня)

- Табы
- Формы
- Валидация

### Phase 7: Integrations (2-3 дня)

- Список
- Добавление/удаление
- Конфигурация

### Phase 8: Polish (2-3 дня)

- Анимации
- Error handling
- Keyboard shortcuts
- Testing

**Общая оценка: 15-23 дня**

---

## 22. Зависимости

- `iced = "0.13"` — UI framework
- `tokio` — async runtime
- `serde` — сериализация
- `chrono` — даты
- `parking_lot` — синхронизация

Шрифты: Inter, JetBrains Mono (встроить в бинарник)

---

## 23. Решения по открытым вопросам

1. **Версия iced**: 0.13 ✅
2. **Локализация**: Не в v1 (добавить позже при необходимости)
3. **Light theme**: Да, добавить в v1 ✅
4. **Command Palette**: Да, включить в v1 (Ctrl+K/Ctrl+P) ✅

---

**Конец спецификации**

*Готово для передачи Sonnet в VS Code для реализации.*
