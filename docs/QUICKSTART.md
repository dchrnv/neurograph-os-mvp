# NeuroGraph OS - Quick Start Guide

Быстрый старт для работы с NeuroGraph OS - высокопроизводительной системой пространственных вычислений на основе токенов.

---

## 🚀 Быстрый запуск (5 минут)

### 1. Установка зависимостей

```bash
# Rust (если еще не установлен)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# Обновить Rust до последней версии
rustup update stable
```

### 2. Клонировать и собрать проект

```bash
git clone <repository-url>
cd neurograph-os-mvp

# Собрать все компоненты
cargo build --release
```

### 3. Запустить Desktop UI

```bash
# Из корня проекта
cargo run --manifest-path src/desktop/Cargo.toml --release
```

**Готово!** Откроется графический интерфейс NeuroGraph OS.

---

## 🔐 Вход в систему

При первом запуске вы увидите экран аутентификации с PIN-кодом:

- **User PIN**: `1234` (обычный пользователь)
- **Root PIN**: `0000` (администратор с полным доступом)

Введите 4-6 цифр и нажмите **User** или **Root**.

---

## 📱 Интерфейс

### Навигация (боковая панель слева)

- **□ Dashboard** - Обзор системы с метриками
- **💬 Chat** - Командный интерфейс / Терминал
- **◧ Modules** - Управление модулями
- **☰ Logs** - Просмотр системных логов
- **⚙ Settings** - Настройки системы
- **⊕ Integrations** - Внешние интеграции
- **! Admin** - Админ-панель (только для root)

### Dashboard экран

Показывает ключевые метрики:
- **TOKENS** - количество токенов в системе
- **CONNECTIONS** - активные соединения
- **MEMORY** - использование памяти
- **THROUGHPUT** - пропускная способность
- **LATENCY** - задержка
- **UPTIME** - время работы

**Quick Actions**:
- 💬 New Chat - открыть чат
- 📋 View Logs - просмотр логов
- 🔄 Restart All - перезапуск всех модулей
- ⚙ Export Config - экспорт конфигурации

### Chat экран (две вкладки)

#### 📝 Chat режим
Обычный чат-интерфейс с историей сообщений.

#### 💻 Terminal режим
Терминальный интерфейс с prompt:
- `user@neurograph $` - обычный пользователь
- `root@neurograph #` - администратор

---

## 💬 Доступные команды в Chat/Terminal

### Основные команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `status` / `статус` | Статус системы (токены, граф) | `status` |
| `help` / `помощь` / `?` | Справка по командам | `help` |

### Работа с токенами

| Команда | Описание | Пример |
|---------|----------|--------|
| `create` / `создать токен` | Создать 1 токен | `create` |
| `create N` | Создать N токенов (max 100) | `create 10` |
| `list` / `список` | Показать количество токенов | `list` |

### Работа с графом

| Команда | Описание | Пример |
|---------|----------|--------|
| `graph` / `граф` | Информация о графе (узлы, связи) | `graph` |

### Системные команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `clear` / `очистить` | Удалить все токены и связи | `clear` |

### Примеры использования

```bash
# Проверить статус системы
status

# Создать 5 токенов
create 5

# Посмотреть информацию о графе
graph

# Очистить все данные
clear

# Получить справку
help
```

---

## 🛠️ Команды разработки

### Сборка проекта

```bash
# Собрать всё (debug mode)
cargo build

# Собрать с оптимизацией (release mode)
cargo build --release

# Собрать только Desktop UI
cargo build --manifest-path src/desktop/Cargo.toml

# Собрать только Core
cargo build --manifest-path src/core_rust/Cargo.toml
```

### Запуск

```bash
# Запустить Desktop UI (debug)
cargo run --manifest-path src/desktop/Cargo.toml

# Запустить Desktop UI (release)
cargo run --manifest-path src/desktop/Cargo.toml --release

# Запустить с логированием
RUST_LOG=debug cargo run --manifest-path src/desktop/Cargo.toml
```

### Тестирование

```bash
# Запустить все тесты
cargo test

# Тесты только Core
cargo test --manifest-path src/core_rust/Cargo.toml

# Тесты Desktop UI
cargo test --manifest-path src/desktop/Cargo.toml

# Тесты с выводом
cargo test -- --nocapture

# Тесты конкретного модуля
cargo test --package neurograph-core

# Запуск стресс-теста (9.9M токенов)
cargo test --manifest-path src/core_rust/Cargo.toml --release -- stress_test_9_9m_tokens --nocapture --ignored
```

### Проверка кода

```bash
# Проверка без компиляции
cargo check

# Проверка с clippy (линтер)
cargo clippy

# Исправить форматирование
cargo fmt

# Проверить форматирование
cargo fmt -- --check
```

### Очистка

```bash
# Удалить скомпилированные файлы
cargo clean

# Удалить только Desktop UI build
cargo clean --manifest-path src/desktop/Cargo.toml

# Удалить зависимости и пересобрать
cargo clean && cargo build
```

### Документация

```bash
# Сгенерировать и открыть документацию
cargo doc --open

# Документация только для Core
cargo doc --manifest-path src/core_rust/Cargo.toml --open

# Документация без зависимостей
cargo doc --no-deps --open
```

---

## 📦 Структура проекта

```
neurograph-os-mvp/
├── src/
│   ├── core_rust/          # Основная бизнес-логика (Rust)
│   │   ├── src/
│   │   │   ├── lib.rs      # Точка входа библиотеки
│   │   │   ├── token.rs    # Токены
│   │   │   ├── grid.rs     # Пространственная сетка
│   │   │   ├── graph.rs    # Граф связей
│   │   │   ├── guardian.rs # Валидация и CDNA
│   │   │   └── ...
│   │   └── Cargo.toml
│   │
│   └── desktop/            # Desktop UI (iced framework)
│       ├── src/
│       │   ├── main.rs     # Точка входа
│       │   ├── app.rs      # Основное приложение
│       │   ├── auth/       # Аутентификация (PIN)
│       │   ├── core/       # Мост к core_rust
│       │   ├── theme.rs    # Terminal Modern тема
│       │   ├── workspaces/ # Экраны (Dashboard, Chat, etc)
│       │   ├── layout.rs   # Header, Status Bar
│       │   └── metrics.rs  # Метрики визуализация
│       └── Cargo.toml
│
├── docs/
│   └── specs/
│       └── DESKTOP_UI_SPEC_V3.md  # Спецификация UI
│
├── Cargo.toml              # Workspace root
└── QUICKSTART.md           # Этот файл
```

---

## 🎨 UI Дизайн (Terminal Modern)

### Цветовая палитра (GitHub Dark inspired)

- **Фон**: `#0d1117` (BG_PRIMARY), `#161b22` (BG_SECONDARY)
- **Текст**: `#c9d1d9` (PRIMARY), `#8b949e` (SECONDARY)
- **Акцент**: `#58a6ff` (мягкий голубой)
- **Статусы**:
  - ✅ OK: `#3fb950` (зелёный)
  - ⚠️ Warning: `#d29922` (жёлтый)
  - ❌ Critical: `#f85149` (красный)

### Размеры текста

- **XS**: 11px (метки, hints)
- **SM**: 13px (вторичный текст)
- **BASE**: 14px (основной текст)
- **LG**: 16px (заголовки секций)
- **XL**: 20px (заголовки экранов)
- **XXL**: 24px (главные заголовки)
- **DISPLAY**: 32px (логотип)

---

## 🔧 Настройка окружения

### VS Code

Рекомендуемые расширения:
- **rust-analyzer** - поддержка Rust
- **crates** - управление зависимостями
- **Error Lens** - подсветка ошибок

### Переменные окружения

```bash
# Уровень логирования (trace, debug, info, warn, error)
export RUST_LOG=debug

# Бэктрейс при панике
export RUST_BACKTRACE=1

# Полный бэктрейс
export RUST_BACKTRACE=full
```

---

## 📊 Производительность

### Стресс-тесты

Проект включает несколько стресс-тестов:

```bash
# 9.9M токенов (игнорируется по умолчанию)
cargo test --release -- stress_test_9_9m_tokens --nocapture --ignored

# Результаты последнего теста сохранены в:
# docs/stress_test_results_9.9M.md
```

**Текущие результаты** (9.9M токенов):
- ✅ Все тесты пройдены
- ⏱️ Время выполнения: ~30-60 секунд
- 💾 Использование памяти: в пределах нормы

---

## 🐛 Отладка

### Логирование

```bash
# Включить debug логи
RUST_LOG=debug cargo run --manifest-path src/desktop/Cargo.toml

# Логи только от neurograph модулей
RUST_LOG=neurograph=debug cargo run --manifest-path src/desktop/Cargo.toml

# Все trace логи
RUST_LOG=trace cargo run --manifest-path src/desktop/Cargo.toml
```

### Частые проблемы

**Ошибка компиляции iced**:
```bash
# Очистить и пересобрать
cargo clean
cargo build
```

**Проблемы с зависимостями**:
```bash
# Обновить lockfile
cargo update
cargo build
```

**Не находит core_rust**:
```bash
# Проверить workspace структуру
cat Cargo.toml
# Должно быть: members = ["src/core_rust", "src/desktop"]
```

---

## 📝 Разработка нового функционала

### Добавить новую команду в чат

1. Открыть `src/desktop/src/core/mod.rs`
2. Добавить обработку в метод `process_message()`:

```rust
match msg_lower.as_str() {
    // ... существующие команды
    "mycommand" => self.my_new_command(),
    _ => format!("Unknown command: '{}'", msg),
}
```

3. Реализовать метод:

```rust
fn my_new_command(&self) -> String {
    // Ваша логика
    "Command executed!".to_string()
}
```

### Добавить новый экран (Workspace)

1. Открыть `src/desktop/src/workspaces/mod.rs`
2. Добавить в `enum Workspace`:

```rust
pub enum Workspace {
    // ... существующие
    MyNewScreen,
}
```

3. Добавить иконку и label:

```rust
fn icon(&self) -> &'static str {
    match self {
        // ...
        Workspace::MyNewScreen => "★",
    }
}
```

4. Добавить view функцию:

```rust
fn my_new_screen_view<'a>() -> Element<'a, Message> {
    column![
        text("My New Screen")
            .size(text_size::XXL)
            .style(iced::theme::Text::Color(TerminalColors::TEXT_PRIMARY)),
        // ... ваш контент
    ]
    .into()
}
```

5. Добавить в match в `impl WorkspaceView`:

```rust
let content = match self {
    // ...
    Workspace::MyNewScreen => my_new_screen_view(),
};
```

---

## 🚢 Деплой

### Release сборка

```bash
# Оптимизированная сборка
cargo build --release --manifest-path src/desktop/Cargo.toml

# Исполняемый файл будет в:
# target/release/neurograph-desktop
# или
# target/release/neurograph-desktop.exe (Windows)
```

### Кросс-компиляция

```bash
# Для Windows (из Linux/Mac)
rustup target add x86_64-pc-windows-gnu
cargo build --release --target x86_64-pc-windows-gnu

# Для Linux (из Windows/Mac)
rustup target add x86_64-unknown-linux-gnu
cargo build --release --target x86_64-unknown-linux-gnu

# Для macOS (из Linux/Windows)
rustup target add x86_64-apple-darwin
cargo build --release --target x86_64-apple-darwin
```

---

## 📚 Дополнительные ресурсы

- **Спецификация UI**: `docs/specs/DESKTOP_UI_SPEC_V3.md`
- **Результаты тестов**: `docs/stress_test_results_9.9M.md`
- **Iced документация**: https://docs.rs/iced/
- **Rust книга**: https://doc.rust-lang.org/book/

---

## 💡 Советы

1. **Используйте release mode** для тестирования производительности
2. **Проверяйте код перед коммитом**: `cargo fmt && cargo clippy && cargo test`
3. **Логируйте важные операции** для упрощения отладки
4. **Следуйте Terminal Modern дизайну** при добавлении новых UI элементов

---

## 🎯 Следующие шаги

После запуска попробуйте:

1. ✅ Войти с User PIN (1234)
2. ✅ Открыть Dashboard и посмотреть метрики
3. ✅ Создать несколько токенов через Chat: `create 10`
4. ✅ Проверить статус: `status`
5. ✅ Переключиться в Terminal режим
6. ✅ Посмотреть Logs экран
7. ✅ Войти как Root (0000) и посмотреть Admin панель

---

**Версия**: v0.46.0
**Дата обновления**: 2025-12-12
**Лицензия**: AGPL-3.0

🚀 **Готово к работе!**
