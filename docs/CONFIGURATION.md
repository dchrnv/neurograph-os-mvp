# NeuroGraph Configuration Guide

Полное руководство по настройке и конфигурации проекта.

## Архитектура конфигурации

```
neurograph-os-mvp/
├── .config.sh              # Главный конфигурационный скрипт
├── config/                 # Централизованные настройки
│   ├── project.env         # Основные настройки проекта
│   ├── python.env          # Python-специфичные настройки
│   ├── rust.env            # Rust-специфичные настройки
│   └── versions.env        # Версии зависимостей
├── .env.local              # Локальные переопределения (не в git)
└── show-config.sh          # Просмотр текущей конфигурации
```

## Принципы

### 1. Единый источник истины
Все настройки собраны в директории [`config/`](config/), а [`.config.sh`](.config.sh) их объединяет и предоставляет унифицированный API.

### 2. Иерархия настроек
```
config/*.env          # Базовые настройки проекта
    ↓
.env.local            # Локальные переопределения
    ↓
Переменные окружения  # Runtime переопределения
```

### 3. Автоматическое определение
Система автоматически определяет:
- Версию Python (3.12 или 3.13)
- Окружение (Flatpak или standard)
- Платформу (Linux, macOS, Windows)

## Основные настройки

### Порты

```bash
BACKEND_PORT=8000      # FastAPI backend
FRONTEND_PORT=5173     # Vite dev server
PROMETHEUS_PORT=9090   # Metrics
JAEGER_PORT=16686      # Tracing UI
```

### Директории

```bash
PROJECT_ROOT           # Корень проекта
VENV_DIR=".venv"       # Virtual environment
SRC_DIR="src"          # Исходный код
DATA_DIR="data"        # Данные и БД
```

### Фичи

```bash
RUST_CORE_ENABLED=true     # Использовать Rust Core
JUPYTER_ENABLED=true       # Jupyter интеграция
WEBSOCKET_ENABLED=true     # WebSocket поддержка
METRICS_ENABLED=true       # Prometheus метрики
```

## Использование

### Просмотр конфигурации

```bash
./show-config.sh
```

Выведет:
```
📋 NeuroGraph Configuration
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Project:     neurograph v0.63.0
Root:        /home/user/neurograph-os-mvp
Environment: development
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Python:      Python 3.13.7
Flatpak:     true
VirtualEnv:  /home/user/neurograph-os-mvp/.venv
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Backend:     http://localhost:8000
Frontend:    http://localhost:5173
API Docs:    http://localhost:8000/docs
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Features:
  Rust Core:  true
  Jupyter:    true
  WebSocket:  true
  Metrics:    true
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Использование в скриптах

Все скрипты автоматически загружают конфигурацию:

```bash
#!/bin/bash
source "$(dirname "$0")/.config.sh"

# Теперь доступны все переменные
echo "Project: $PROJECT_NAME v$PROJECT_VERSION"
echo "Backend port: $BACKEND_PORT"

# И helper функции
run_cmd "python --version"
get_python_version
print_config
```

### Локальные переопределения

Создайте файл `.env.local` для локальных настроек:

```bash
# .env.local
BACKEND_PORT=9000
ENVIRONMENT="staging"
METRICS_ENABLED=false
```

**ВАЖНО:** `.env.local` не попадает в git (в `.gitignore`)

## Helper функции

### `run_cmd <command>`
Выполняет команду с учётом окружения (Flatpak/standard):

```bash
run_cmd "pip install numpy"
run_cmd ".venv/bin/python script.py"
```

### `get_python_version`
Возвращает версию Python:

```bash
VERSION=$(get_python_version)
echo "Using: $VERSION"
```

### `print_config`
Выводит сводку текущей конфигурации.

### `check_venv`
Проверяет наличие virtual environment:

```bash
if check_venv; then
    echo "VEnv exists"
fi
```

### `activate_venv`
Активирует virtual environment:

```bash
activate_venv && python script.py
```

## Редактирование конфигурации

### Изменение версии проекта

Отредактируйте [`config/project.env`](config/project.env):

```bash
PROJECT_VERSION="0.64.0"
```

### Добавление новой настройки

1. Добавьте в соответствующий файл в [`config/`](config/):
   ```bash
   # config/project.env
   NEW_FEATURE_ENABLED=true
   ```

2. Используйте в скриптах:
   ```bash
   if [ "$NEW_FEATURE_ENABLED" = true ]; then
       echo "Feature enabled"
   fi
   ```

### Добавление версии зависимости

Отредактируйте [`config/versions.env`](config/versions.env):

```bash
NUMPY_VERSION=">=2.0.0"
```

## Troubleshooting

### Конфигурация не загружается

Проверьте что скрипт загружает `.config.sh`:

```bash
source "$(dirname "$0")/.config.sh"
```

### Настройки не применяются

1. Проверьте иерархию (`.env.local` переопределяет `config/*.env`)
2. Используйте `./show-config.sh` для диагностики
3. Убедитесь что переменные экспортируются (`export VAR=value`)

### Python версия неправильная

Система автоматически определяет доступную версию Python.
Для переопределения создайте `.env.local`:

```bash
PYTHON_CMD="python3.12"
```

## Best Practices

1. **Никогда не коммитьте** `.env.local` или файлы с секретами
2. **Документируйте** новые настройки в соответствующих `*.env` файлах
3. **Используйте** осмысленные имена переменных (FEATURE_ENABLED, не FEAT)
4. **Группируйте** связанные настройки (все порты вместе, все пути вместе)
5. **Добавляйте** комментарии для сложных настроек

## См. также

- [README.md](README.md) - Основная документация
- [SCRIPTS.md](SCRIPTS.md) - Описание скриптов
- [config/README.md](config/README.md) - Детали конфигурационных файлов
