"""
Setup script for neurograph-python package.

For development installation:
    pip install -e .
    pip install -e ".[dev]"

For production installation:
    pip install neurograph-python
"""

from setuptools import setup

# Configuration is in pyproject.toml
setup()
